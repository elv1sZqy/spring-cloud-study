server:
  port: 3333
spring:
  application:
    name: ribbon-consumer
eureka:
  client:
    service-url:
      defaultZone: http://${eureka.instance.hostname}:1111/eureka/
  instance:
    hostname: localhost
